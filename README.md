***Local machine setup***:

1. git clone git@github.com:arpitbbhayani/tcpserver.git

2. cd to the codebase

3. Run this command: python main.py

4. In another terminal run this command: 
curl http://localhost:1729 &